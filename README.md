# infinite-canvas
An infinite zoom drawing canvas built with the native Canvas 2D API.

Open `index.html` in a modern browser. Draw with the primary mouse button or a pen, pan with the right mouse button, and zoom continuously with the mouse wheel or a two-finger pinch gesture. Stroke width remains constant on screen at every zoom level.
